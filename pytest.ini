# pytest.ini
[pytest]
addopts = --allow-unix-socket --force-enable-socket --allow-hosts=127.0.0.1,152.88.10.20
markers =
    integration: mark live-API integration tests
    e2e: mark end-to-end tests against a running Home Assistant instance
testpaths = tests/test_config_flow.py
